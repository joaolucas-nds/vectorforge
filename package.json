{
  "name": "vectorforge",
  "version": "0.2.0",
  "private": true,
  "description": "Gerador procedural de arte vetorial no browser — arquivo único, zero dependências de runtime.",
  "scripts": {
    "build": "node scripts/build.js",
    "watch": "node scripts/build.js --watch"
  },
  "devDependencies": {
    "esbuild": "^0.24.2"
  }
}
